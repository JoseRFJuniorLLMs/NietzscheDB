[package]
name = "nietzsche-pq"
version = "0.1.0"
edition = "2021"
description = "Product Quantization for NietzscheDB — magnitude-preserving vector compression"

[dependencies]
serde = { workspace = true }
serde_json = "1.0"
bincode = "1"
thiserror = { workspace = true }
tracing = { workspace = true }
rand = "0.8"

[dev-dependencies]
tempfile = "3"
