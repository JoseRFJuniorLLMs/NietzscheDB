# ── Build ──
/target/
**/target/

# ── Environment ──
.env
*.env

# ── Python ──
sdks/python/venv
__pycache__
**/*.pyc
**/venv/
venv/

# ── OS ──
.DS_Store
nul

# ── Cargo ──
Cargo.lock

# ── Data files ──
**/*.hyp
**/*.snap
**/*.bin
**/*.tar.gz

# ── Logs (tracked in logs/ for reference, but ignore new ones at root) ──
*.log
*.log.*

# ── Generated experiment artifacts ──
experiments/*.csv
experiments/*.png

# ── Directories ──
data/
node_modules/
dist/

# ── Other ──
**/TODO*.md
package-lock.json
